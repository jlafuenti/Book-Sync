# Shell scripts must stay LF so they run in the Linux containers regardless of
# the checkout platform (a CRLF shebang breaks `sh entrypoint.sh`).
*.sh text eol=lf
server/entrypoint.sh text eol=lf

# The pinned interpreter version read by uv and setup-testenv.sh.
.python-version text eol=lf
