[pytest]
# Run from the server/ directory: `cd server && pytest`
asyncio_mode = auto
testpaths = tests
# conftest.py also inserts the server dir into sys.path, so the suite runs
# regardless of the working directory (matches the existing sys.path.insert
# pattern the pre-existing tests use).
pythonpath = .
# Surface our own deprecations (they show in the warnings summary). Only silence
# known noise we don't control or have already triaged. Don't blanket-ignore.
filterwarnings =
    ignore::DeprecationWarning:passlib
    ignore::DeprecationWarning:pkg_resources
    # datetime.utcnow() is gone from OUR code (issue #52) — every timestamp
    # comes from utils.utcnow(). This stays an `ignore` because the pinned
    # python-jose 3.3.0 still calls it internally (jose/jwt.py), and a runtime
    # filter cannot tell our frames from a dependency's. The real gate is
    # static: tests/test_time_contract.py walks the AST of every file under
    # server/ and fails on a reintroduced call, which is broader than a
    # runtime filter anyway (it catches code no test happens to execute).
    ignore:datetime.datetime.utcnow:DeprecationWarning
