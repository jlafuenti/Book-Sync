# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# (httpx is already a prod dependency and provides ASGITransport + AsyncClient.)
pytest==8.3.*
pytest-asyncio==0.24.*
# 7.x, not 5.x. #405 closed on the belief that pytest-cov 7 tripled the suite;
# measured A/B on an idle machine with the CI invocation, it does not — 31m52s
# under 7.1.0 against 32m46s under 5.0.0, i.e. marginally faster. The original
# comparison was between runs under different machine load, and the CI symptom
# coincided with every suite running twice per push, which #455 removed. See
# #451 for the numbers.
pytest-cov==7.1.*
pytest-watch==4.2.*
diff-cover==10.*
aiosqlite==0.22.*
# services.alignment imports numpy directly; in production it arrives transitively
# via torch/whisper, but the test suite runs without that heavy stack, so pin it here
# to keep the pre-existing alignment tests runnable in CI.
numpy
