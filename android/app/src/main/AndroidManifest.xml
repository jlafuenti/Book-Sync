<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".BookSyncApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Tandem"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tandem">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Tandem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- EPUB Reader Activity (Readium).
             configChanges: the process-death guard in onCreate finishes the
             activity on ANY recreation (it cannot restore the reflective
             EpubNavigatorFragment state), so recreation-causing config changes
             are handled in place instead — otherwise the first rotation,
             dark-mode toggle or split-screen closed the book (issue #163).
             The reader's theme is its own preference, not uiMode, and
             Readium's WebView re-lays itself out. Do NOT swap this for an
             orientation lock: Android 16 ignores orientation restrictions on
             large screens. -->
        <activity
            android:name=".ui.reader.ReaderActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|fontScale|density|locale|layoutDirection"
            android:theme="@style/Theme.Tandem" />

        <!-- Audio Playback + Android Auto Library Service (single consolidated service) -->
        <service
            android:name=".player.AudioPlayerService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- FileProvider for serving cover art content:// URIs to Android Auto -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Diagnostic log stop action from persistent notification -->
        <receiver
            android:name=".diagnostics.DiagnosticsStopReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.booksync.action.STOP_DIAGNOSTICS" />
            </intent-filter>
        </receiver>

        <!-- WorkManager Data Sync Service -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Android Auto support -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Google Cast options provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.booksync.cast.CastOptionsProvider" />

        <!-- WorkManager Initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
