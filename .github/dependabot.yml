version: 2

# Dependency updates (issue #189). Four manifests live in this repo and each one
# ships code: the server's pip requirements, the web app's npm tree, the Android
# Gradle catalog, and the actions CI runs. Nothing here told the maintainer when
# one of them picked up a published advisory.
#
# Weekly and grouped on purpose: a routine week should be one PR per ecosystem,
# not twenty. Security updates are exempt from the group cap and still arrive on
# their own as soon as GitHub knows about them.
#
# This file only opens PRs. Turn on Dependabot alerts and security updates in
# Settings -> Code security, or the security half of this never fires.
updates:
  - package-ecosystem: "pip"
    directory: "/server"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      server-minor-and-patch:
        update-types: ["minor", "patch"]

  # The transcription worker's own requirements. Not in the same group as the
  # server: it runs on different hardware and is upgraded independently.
  - package-ecosystem: "pip"
    directory: "/jetson"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 3
    groups:
      jetson-minor-and-patch:
        update-types: ["minor", "patch"]

  - package-ecosystem: "npm"
    directory: "/web"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      web-minor-and-patch:
        update-types: ["minor", "patch"]

  - package-ecosystem: "gradle"
    directory: "/android"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      android-minor-and-patch:
        update-types: ["minor", "patch"]

  # Keeps the SHA pins in .github/workflows/ current. Without this, pinning to a
  # commit means the actions silently stop receiving upstream fixes — the pins
  # and this ecosystem are one mechanism, not two.
  # Base images are shipped dependencies too. Without these blocks the web image
  # sat on a Node line five months past its end of life and nothing said so.
  # Majors are proposed as single PRs like any other ecosystem; a Postgres major
  # is deliberately never proposed, because Watchtower pulls the demo's database
  # image unattended and a major jump would strand its data volume.
  - package-ecosystem: "docker"
    directory: "/server"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 2
    ignore:
      # The Python line is pinned three ways (server/.python-version, CI's
      # python-version, this image) and moves in one hand-made PR — the suite
      # has to have run on it first. Patch tags may still arrive.
      - dependency-name: "python"
        update-types: ["version-update:semver-major", "version-update:semver-minor"]
  - package-ecosystem: "docker"
    directory: "/web"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 2
    ignore:
      # Node stays on the active LTS line, which CI also runs; the bot offers
      # the newest Current line (26) the moment it exists. Move it by hand with
      # web-tests.yml when the next LTS lands.
      - dependency-name: "node"
        update-types: ["version-update:semver-major"]
  - package-ecosystem: "docker"
    directory: "/jetson"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 1
  - package-ecosystem: "docker-compose"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 2
    ignore:
      - dependency-name: "postgres"
        update-types: ["version-update:semver-major"]
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    groups:
      actions-minor-and-patch:
        update-types: ["minor", "patch"]
